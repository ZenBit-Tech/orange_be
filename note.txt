to run docker in redis 
docker run -d \
  --name redis \
  -p 6379:6379 \
  -v redis_data:/data \
  redis:latest \
  redis-server --appendonly yes

  docker ps

  docker exec -it redis redis-cli

  127.0.0.1:6379> KEYS myapp:*
